<!DOCTYPE html>
<html>
  <%= render "partials/head" %>
  <body>
    <%= render "partials/admin_header" %>
    <div class="container">
      <% unless notice.nil? %>
      <p class="alert-info"><%= notice %></p>
      <% end %>
      <% unless alert.nil? %>
      <p class="alert-info"><%= alert %></p>
      <% end %>

      <div class="title">
        <h2>
          <%= link_to 'Análisis de datos', data_analysis_index_path() %>
          <%= ": #{@analysis_name}" unless @analysis_name.blank? %>
        </h2>
      </div>

      <div class="panel panel-default panel-page">
        <div class="panel-body">
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
