<div class="entity-content <%= controller_name %>">

  <div class="title entity single">
    <h2><%= @title %></h2>
  </div>

  <div class="sharing-right">
    <%= render 'partials/sharing_buttons', title: @title %>
  </div>

  <div class="entity-description">
    <% if controller.controller_name == 'bidders' %>
      <strong>De todas las adjudicaciones publicadas en el BOE entre 2009 y 2015, <%= @title %></strong> se ha llevado de forma directa <%= @contract_awards.count %> contratos por un importe de X €. <br/>
      Además, ha formado parte de UTEs que resultaron adjudicatarias de otros X contratos y que suman otros X euros. <br/>
      Así, <%= @title %> ha estado presente, en total, en la adjudicación de X contratos y X euros. De esa cantidad, un X% del importe fue adjudicado vía procedimiento negociado. Entre las administraciones que le contratan, la que tiene mayor peso es X (X % del total del importe adjudicado tanto de forma directa como a través de las UTEs en las que participa).
    <% else %>
      <strong>De todas las adjudicaciones publicadas en el BOE entre 2009 y 2015, <%= @title %></strong> ha adjudicado <%= @contract_awards.count %> contratos por un importe de X €, de los cuales un X % ha sido por procedimiento negociado. <br/>
      El principal grupo empresarial beneficiado ha sido X (está presente, de forma directa o a través de UTEs, en el X % del total del importe adjudicado)
    <% end %>
  </div>

  <% if @contract_awards.length > 1 %>
  <div class="row entity-charts">
    <div class="col-md-6">
      <div class="entity-chart-title">
        <% if controller.controller_name == 'bidders' %>
        <h3>Reparto de contratos por Administración pública</h3>
        <% else %>
        <h3>Reparto de contratos por Grupo constructor</h3>
        <% end %>
        <div class="utes-switch-container">
          <input id="utes-switch" class="utes-switch" type="checkbox" checked data-size="small" data-on-text="Con UTEs" data-off-text="Sin UTEs">
        </div>
      </div>
      <div id="treemap-chart" class="entity-chart">
        <div class='popover top' role='tooltip'>
          <div class='arrow'></div>
          <div class='popover-title'></div>
          <div class='popover-budget'><strong></strong> € <span class="percentage"></span></div>
        </div>
      </div>
      <div class="well">Texto explicativo del gráfico superior</div>
    </div>
    <div class="col-md-6">
      <div class="entity-chart-title">
        <% if controller.controller_name == 'bidders' %>
        <h3>Evolución temporal de los contratos recibidos</h3>
        <% else %>
        <h3>Evolución temporal de los contratos adjudicados</h3>
        <% end %>
      </div>
      <div id="timeline-bar-chart" class="entity-chart">
        <div class='popover top' role='tooltip'>
          <div class='arrow'></div>
          <div class='popover-title'><i class="glyphicon glyphicon-calendar"></i><span></span></div>
          <div class='popover-budget'><strong></strong> €</div>
        </div>
      </div>
      <div class="well">Texto explicativo del gráfico superior</div>
    </div>
  </div>
  <% end %>

  <div>
    <% if controller.controller_name == 'bidders' %>
    <h3>Contratos adjudicados a <%= @title %></h3>
    <% else %>
    <h3>Contratos adjudicados por <%= @title %></h3>
    <% end %>
    <div class="panel panel-default">
      <%= render('partials/contract_awards_table', awards: @contract_awards) %>
    </div>
  </div>

  <div>
    <% if controller.controller_name == 'bidders' %>
    <h3>Contratos adjudicados a UTEs en las que participa <%= @title %></h3>
    <div class="panel panel-default">
      <%= render('partials/contract_awards_table', awards: @contract_awards, utes: true) %>
    </div>
    <% end %>
  </div>

  <div class="panel panel-default">
    <iframe src="http://quienmanda.es/entities/ferrovial?widget=1&history=" width="100%" height="450px" scrolling="no" marginheight="0" frameborder="0"></iframe>
  </div>

</div>